# Phase 5e Prompt: Query Entity Extraction (Graph-RAG)

**Source**: `src/code_p5e_graph_rag.py` (`QUERY_ENTITY_PROMPT`)

---

```
从以下用户查询中提取涉及的关键实体（技术名词、工具名、系统名、概念等）。
直接返回 JSON 格式：{"entities": ["实体1", "实体2", ...]}

查询: {query}

返回 JSON:
```
