# OpenCode Session 知识图谱与跨 Session 检索系统

> 需求 + 实施方案 v2.0
> 最后更新:2026-07-13

---

## 1. 项目概述

### 1.1 背景

OpenCode 在长期使用中会积累大量 session 历史,分散在不同的 root session 中,无法进行统一检索、归纳、关联。用户希望将这些历史沉淀为可挖掘的知识资产。

### 1.2 目标

构建一套基于 OpenCode session 历史的知识管理系统,提供三个核心能力:

1. **跨 session 搜索**:用自然语言查询,召回历史 session 中相关的对话轮次
2. **跨 session 总结**:基于多个相关 session,生成主题性总结(如"我最近一个月是怎么处理登录模块的")
3. **知识图谱**:从 session 中抽取实体与关系,构建可查询、可视化的知识网络

### 1.3 范围

- **模态**:文本为主,中英文混合,图片可有可无
- **数据范围**:仅处理 root session,且对话轮数 > 1(至少 2 个真实 user 输入)
- **不包含**:图片/附件内容识别(本版本)

### 1.4 名词解释

| 术语 | 含义 |
|------|------|
| Root Session | OpenCode 的根会话,不是分支 session |
| 对话轮次(Turn / Chunk) | 1 个 user 消息 + 该 user 触发的所有 assist/工具/MCP 调用 |
| Task | Session 内完成的"一件事",可能跨多个轮次 |
| Summary | Task 或 Chunk 的语义摘要 |
| 实体 | 知识图谱中的节点(人名、项目名、概念、文件等) |
| 三元组 | 知识图谱中的边表示:(实体, 关系, 实体) |

---

## 2. 核心场景

### 2.1 跨 Session 搜索

**用户输入**:
> "我之前是怎么修复 token 过期导致登录失败的?"

**系统行为**:
1. Query Embedding
2. Qdrant 检索 Top-K Task 摘要
3. Reranker 重排
4. 通过 chunk_id 关联回到原文
5. 返回相关 session + 关键轮次

### 2.2 跨 Session 总结

**用户输入**:
> "总结一下我最近做过的所有性能优化工作"

**系统行为**:
1. Query Embedding
2. 检索相关 Task 摘要(可加时间范围、主题范围过滤)
3. 汇总 Task 摘要 + 关联 Chunk 原文
4. LLM 生成主题总结

### 2.3 知识图谱

**用户操作**:
- 可视化浏览:项目涉及的所有人、文件、概念
- 图查询:找出与"登录模块"相关联的所有实体
- 实体追溯:点击"修复 token 过期 bug"这个任务,回到原始 session

---

## 3. 整体架构

### 3.1 系统流程图

```
┌──────────────────────────────────────────────────────────┐
│  OpenCode Root Session (turn_count > 1)                  │
└────────────────────┬─────────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────────┐
│  Step 1: 轮次切分                                          │
│  - 边界 = user 消息出现位置                                │
│  - chunk = 1 user + 多 assist/工具/MCP 调用              │
└────────────────────┬─────────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────────┐
│  Step 2: 内容整理(降噪 + 减体积)                          │
│  - user/assist: 完整保留                                  │
│  - bash:     { "cmd": "...", "ok": true/false }          │
│  - 工具调用: { "action": "...", "ok": true/false }        │
│  - MCP:      { "fetch": "...", "ok": true/false }         │
└────────────────────┬─────────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────────┐
│  Step 3: Session 级 Task 清单生成                         │
│  LLM 整 session 视角,输出 1-5 个 Task(大多 1-2 个)       │
│  每个 task: task_id, task_label, task_summary,            │
│            chunk_ids(该任务涉及的轮次)                     │
└────────────────────┬─────────────────────────────────────┘
                     │
        ┌────────────┴────────────┐
        ▼                         ▼
┌────────────────┐       ┌─────────────────────┐
│  Step 4a:      │       │  Step 4b:           │
│  Chunk         │       │  Task               │
│  Embedding     │       │  Embedding          │
│  → Qdrant_C    │       │  → Qdrant_T         │
└────────────────┘       └──────────┬──────────┘
                                    │
                                    ▼
                        ┌──────────────────────────┐
                        │  Step 5: 知识图谱          │
                        │  - Task 摘要 → LLM 抽三元组│
                        │  - Embedding 实体对齐      │
                        │  - 写入 NetworkX          │
                        │  (后期可迁 Neo4j)         │
                        └──────────────────────────┘
                                    │
        ┌───────────────────────────┴────────────────────┐
        ▼                                                    ▼
┌──────────────────┐                              ┌──────────────────┐
│  跨 session 搜索  │                              │  跨 session 总结  │
│  query→Qdrant_T  │                              │  TopK Task→LLM  │
│  →Reranker       │                              │  生成总结         │
│  →Qdrant_C 原文   │                              │                  │
└──────────────────┘                              └──────────────────┘
```

### 3.2 核心模块

| 模块 | 职责 |
|------|------|
| `session_loader` | 读取 OpenCode session 原始数据 |
| `chunker` | 按轮次切分 session |
| `content_cleaner` | 内容整理(降噪、减体积) |
| `task_extractor` | 调用 LLM 抽取 Session 级 Task 清单 |
| `embedder` | 调用 Embedding 模型生成向量 |
| `qdrant_store` | 向量数据库读写 |
| `reranker` | Reranker 模型推理 |
| `kg_builder` | 知识图谱构建(三元组抽取、实体对齐、图写入) |
| `searcher` | 跨 session 搜索流程编排 |
| `summarizer` | 跨 session 总结流程编排 |
| `config` | 统一配置管理 |

---

## 4. 数据模型

### 4.1 输入数据:OpenCode Session

OpenCode session 通常以 JSON / JSONL 形式存储,典型结构:

```json
{
  "id": "ses_1022f744bffeyKVYzPqgjIfv5k",
  "type": "root",
  "created_at": "2026-07-10T10:00:00Z",
  "turns": [
    {
      "role": "user",
      "content": "帮我修一下登录 bug",
      "timestamp": "2026-07-10T10:00:00Z"
    },
    {
      "role": "assistant",
      "content": "好的,我先看看代码",
      "tool_calls": [
        {
          "type": "bash",
          "cmd": "cat src/auth.py",
          "ok": true,
          "output": "..."
        }
      ]
    },
    {
      "role": "tool_result",
      "tool_call_id": "...",
      "output": "..."
    }
  ]
}
```

> **注**:实际数据格式以 OpenCode 导出格式为准,需在 Phase 1 实现时确认。

### 4.2 Chunk(轮次)

```python
@dataclass
class Chunk:
    chunk_id: str            # "{session_id}_c{index}", e.g. "abc_c1"
    session_id: str
    turn_index: int          # 在 session 中的轮次序号(从 1 开始)
    user_message: str        # 完整保留
    assistant_messages: list # 完整保留
    tool_calls: list         # 整理后: [{action, ok}, ...]
    mcp_calls: list          # 整理后: [{fetch, ok}, ...]
    raw_size_tokens: int     # 原始 token 数
    cleaned_size_tokens: int # 整理后 token 数
    created_at: str
```

### 4.3 Task

```python
@dataclass
class Task:
    task_id: str             # "{session_id}_T{index}", e.g. "abc_T1"
    session_id: str
    task_label: str          # 简短中文,如"修复登录 bug"
    task_summary: str        # 任务总结
    chunk_ids: list[str]     # 关联的 Chunk ID
    created_at: str
```

### 4.4 Qdrant 存储格式

#### 4.4.1 Qdrant_T(Task 集合,粗粒度)

**向量维度**:1024(Qwen3-Embedding-0.6B 输出维度)

**Payload**:
```json
{
  "task_id": "abc_T1",
  "session_id": "abc",
  "task_label": "修复登录 bug",
  "task_summary": "用户反馈登录失败,排查后发现是 token 过期逻辑错误,修复后验证通过",
  "chunk_ids": ["abc_c1", "abc_c2", "abc_c3"],
  "round_count": 3,
  "created_at": "2026-07-10T10:30:00Z"
}
```

#### 4.4.2 Qdrant_C(Chunk 集合,细粒度)

**Payload**:
```json
{
  "chunk_id": "abc_c1",
  "session_id": "abc",
  "turn_index": 1,
  "task_id": "abc_T1",
  "content_cleaned": "...",   // 整理后的内容
  "content_summary": "...",    // 该轮次的简短总结
  "created_at": "2026-07-10T10:00:00Z"
}
```

### 4.5 知识图谱数据(NetworkX)

```python
# 节点
node = {
  "id": "用户登录模块",        # 实体名(归一后)
  "type": "module",            # 实体类型
  "embedding_id": "uuid",      # 对应 Qdrant 中的实体向量 ID
  "source_tasks": ["abc_T1"],  # 来源 task
  "aliases": ["login", "登录"]  # 别名(归一前收集)
}

# 边(关系)
edge = {
  "source": "用户登录模块",
  "target": "token 过期 bug",
  "relation": "包含_bug",
  "weight": 0.85,              # 来源 LLM 抽取的置信度
  "source_task": "abc_T1"
}
```

---

## 5. 详细设计

### 5.1 数据预处理

#### 5.1.1 Session 过滤

```python
def filter_sessions(sessions: list) -> list:
    """仅处理 root session 且 turn_count > 1"""
    return [
        s for s in sessions
        if s["type"] == "root" and count_user_messages(s) > 1
    ]
```

#### 5.1.2 轮次切分

```python
def split_into_chunks(session: dict) -> list[Chunk]:
    """
    切分规则:
    - 轮次边界 = user 消息出现位置
    - 每个 chunk = 1 user 消息 + 后续所有非 user 消息(直到下一个 user)
    """
    chunks = []
    current_chunk = None
    for turn in session["turns"]:
        if turn["role"] == "user":
            if current_chunk:
                chunks.append(current_chunk)
            current_chunk = new_chunk(session["id"], turn)
        else:
            current_chunk.accumulate(turn)
    if current_chunk:
        chunks.append(current_chunk)
    return chunks
```

#### 5.1.3 内容整理

```python
def clean_chunk(chunk: Chunk) -> Chunk:
    """
    整理规则:
    - user/assistant 文本: 完整保留
    - bash 调用:    { "cmd": "原命令", "ok": true/false }
    - 工具调用:     { "action": "做了什么", "ok": true/false }
    - MCP 调用:     { "fetch": "获取什么", "ok": true/false }
    - 失败调用:     保留 error 摘要(前 200 字)
    """
    chunk.user_message = chunk.user_message
    chunk.assistant_messages = chunk.assistant_messages
    chunk.tool_calls = [
        {"action": tc.summary(), "ok": tc.ok}
        for tc in chunk.tool_calls
    ]
    chunk.mcp_calls = [
        {"fetch": mc.summary(), "ok": mc.ok}
        for mc in chunk.mcp_calls
    ]
    return chunk
```

#### 5.1.4 32K 兜底

```python
MAX_TOKENS = 30000  # 留 buffer 给 prompt + 输出

def safe_truncate(content: str, max_tokens: int = MAX_TOKENS) -> str:
    """
    Token 超限时截断策略:
    1. 优先保留 user 消息
    2. 其次保留 assistant 关键结论
    3. 工具调用结果全部摘要化(已在 Step 2 完成)
    4. 仍然超限则按比例截断中间部分,保留首尾
    """
    if count_tokens(content) <= max_tokens:
        return content
    # 实际实现可采用 tiktoken 切分
    return truncate_with_priority(content, max_tokens)
```

### 5.2 Task 清单生成(Step 3)

#### 5.2.1 Prompt 模板

```python
SESSION_TASK_PROMPT = """你是一个对话历史分析助手。请阅读以下一个 opencode session 的完整对话(已按"轮次"切分,每个轮次包含 1 个 user 消息及其触发的所有操作)。

你的任务是:总结该 session 完成的所有主要工作/任务,粒度为"一件独立的事",而不是"一轮对话"。

【重要约束】
- 同一件事可能跨多个对话轮次
- 大多数 session 包含 1-2 个任务,极少超过 5 个
- 任务之间应该是相对独立的工作,不是同一件事的子步骤

【输出格式】严格 JSON,不要其他内容:
{{
  "tasks": [
    {{
      "task_id": "T1",
      "task_label": "简短中文标签(5-15 字)",
      "task_summary": "该任务的总结(50-200 字,说清楚做了什么、结果如何)",
      "chunk_ids": ["c1", "c2", "c3"]
    }}
  ]
}}

【轮次内容】
{turns_content}
"""
```

#### 5.2.2 解析与校验

```python
def parse_tasks(llm_output: str, session_id: str) -> list[Task]:
    """
    校验:
    - JSON 格式合法
    - tasks 数量在 [1, 5]
    - 所有 chunk_ids 都对应到实际 chunk
    - 每个 chunk 至少属于一个 task(不遗漏)
    - 不允许一个 chunk 同时属于多个 task
    """
    data = json.loads(llm_output)
    tasks = data["tasks"]
    
    # 校验数量
    assert 1 <= len(tasks) <= 5, f"任务数量异常: {len(tasks)}"
    
    # 校验 chunk 覆盖
    all_chunks_in_tasks = set()
    for t in tasks:
        all_chunks_in_tasks.update(t["chunk_ids"])
    # ... 校验完整性
    
    return [
        Task(
            task_id=f"{session_id}_{t['task_id']}",
            session_id=session_id,
            task_label=t["task_label"],
            task_summary=t["task_summary"],
            chunk_ids=[f"{session_id}_{cid}" for cid in t["chunk_ids"]],
            created_at=now()
        )
        for t in tasks
    ]
```

#### 5.2.3 失败重试

```python
@retry(max_attempts=3, backoff=2)
def extract_tasks_with_retry(session_content: str, session_id: str) -> list[Task]:
    """LLM 调用失败时指数退避重试"""
    ...
```

### 5.3 向量化与存储(Step 4)

#### 5.3.1 Embedding 模型加载

```python
# 使用 Qwen3-Embedding-0.6B
# 推荐使用 sentence-transformers 加载
from sentence_transformers import SentenceTransformer

embedder = SentenceTransformer("Qwen/Qwen3-Embedding-0.6B")
EMBEDDING_DIM = 1024
```

#### 5.3.2 Qdrant 初始化

```python
from qdrant_client import QdrantClient
from qdrant_client.models import Distance, VectorParams

client = QdrantClient(host="localhost", port=6333)

# Task 集合
client.create_collection(
    collection_name="tasks",
    vectors_config=VectorParams(size=EMBEDDING_DIM, distance=Distance.COSINE),
)

# Chunk 集合
client.create_collection(
    collection_name="chunks",
    vectors_config=VectorParams(size=EMBEDDING_DIM, distance=Distance.COSINE),
)
```

#### 5.3.3 批量 Embedding 与写入

```python
def embed_and_store_tasks(tasks: list[Task]):
    """Task 摘要向量化并写入 Qdrant_T"""
    texts = [t.task_summary for t in tasks]
    vectors = embedder.encode(texts, batch_size=16, show_progress_bar=True)
    
    points = [
        PointStruct(
            id=t.task_id,
            vector=v.tolist(),
            payload=asdict(t)
        )
        for t, v in zip(tasks, vectors)
    ]
    client.upsert(collection_name="tasks", points=points)

def embed_and_store_chunks(chunks: list[Chunk]):
    """Chunk 向量化并写入 Qdrant_C"""
    texts = [summarize_chunk(c) for c in chunks]
    vectors = embedder.encode(texts, batch_size=16, show_progress_bar=True)
    
    points = [
        PointStruct(
            id=c.chunk_id,
            vector=v.tolist(),
            payload={
                "chunk_id": c.chunk_id,
                "session_id": c.session_id,
                "turn_index": c.turn_index,
                "task_id": c.task_id,
                "content_cleaned": c.cleaned_text(),
                "content_summary": summarize_chunk(c),
                "created_at": c.created_at,
            }
        )
        for c, v in zip(chunks, vectors)
    ]
    client.upsert(collection_name="chunks", points=points)
```

### 5.4 知识图谱构建(Step 5)

#### 5.4.1 三元组抽取

```python
TRIPLE_EXTRACTION_PROMPT = """你是一个知识图谱构建助手。请从以下"任务摘要"中抽取 (实体, 关系, 实体) 三元组。

【要求】
- 实体:人名、项目名、模块名、文件、技术概念、问题等
- 关系:动词短语,如 "使用"、"依赖"、"修复了"、"属于"、"包含" 等
- 忽略过于通用的实体(如"用户"、"代码"、"系统")
- 输出 3-10 个高质量三元组

【输出格式】严格 JSON:
{{
  "triples": [
    {{"head": "实体1", "relation": "关系", "tail": "实体2", "confidence": 0.9}}
  ]
}}

【任务摘要】
{task_summary}
"""
```

#### 5.4.2 实体对齐

```python
def align_entities(entities: list[str], threshold: float = 0.92) -> dict:
    """
    实体对齐(归一化):
    1. 对每个实体生成 Embedding
    2. 写入 Qdrant 实体集合
    3. 检索 Top-5 相似实体
    4. 相似度 > threshold 的,送 LLM 二次确认
    5. 确认合并的归一到一个 canonical name
    """
    entity_embeddings = embedder.encode(entities)
    
    # 写入/检索 Qdrant
    entity_collection = "entities"
    ensure_collection(entity_collection)
    upsert_entities(entities, entity_embeddings)
    
    # 找候选合并对
    merge_candidates = {}
    for ent in entities:
        similar = client.search(
            collection_name=entity_collection,
            query_vector=embedder.encode(ent),
            limit=5,
            score_threshold=threshold
        )
        for hit in similar:
            if hit.id != ent and hit.score >= threshold:
                pair = tuple(sorted([ent, hit.id]))
                merge_candidates[pair] = hit.score
    
    # LLM 二次确认
    canonical_map = {}
    for (e1, e2), score in merge_candidates.items():
        if llm_confirm_merge(e1, e2):  # 询问 LLM 是否同一实体
            canonical = choose_canonical_name(e1, e2)
            canonical_map[e1] = canonical
            canonical_map[e2] = canonical
    
    return canonical_map
```

#### 5.4.3 图谱写入(NetworkX)

```python
import networkx as nx

def build_knowledge_graph(tasks: list[Task], triples: list[dict], 
                         entity_alignment: dict):
    G = nx.MultiDiGraph()
    
    # 写入节点
    for entity, canonical in entity_alignment.items():
        if canonical not in G:
            G.add_node(canonical, type=infer_type(canonical))
    
    # 写入边
    for triple in triples:
        head = entity_alignment.get(triple["head"], triple["head"])
        tail = entity_alignment.get(triple["tail"], triple["tail"])
        G.add_edge(
            head, tail,
            relation=triple["relation"],
            weight=triple["confidence"],
            source_task=triple.get("source_task")
        )
    
    return G

# 持久化
nx.write_gpickle(G, "knowledge_graph.gpickle")
```

### 5.5 跨 Session 搜索

```python
def search_sessions(query: str, top_k: int = 5) -> list[dict]:
    """
    流程:
    1. Query Embedding
    2. Qdrant_T 检索 Top-K*5 个 Task
    3. Reranker 重排取 Top-K
    4. 通过 chunk_ids 回到 Qdrant_C 拿原文
    """
    # 1. Embedding
    query_vector = embedder.encode(query)
    
    # 2. 粗排
    candidates = client.search(
        collection_name="tasks",
        query_vector=query_vector,
        limit=top_k * 5
    )
    
    # 3. Reranker 重排
    reranked = reranker.rank(
        query=query,
        documents=[c.payload["task_summary"] for c in candidates],
        top_k=top_k
    )
    
    # 4. 取回原文
    results = []
    for hit in reranked:
        task = candidates[hit.idx].payload
        chunks = client.retrieve(
            collection_name="chunks",
            ids=task["chunk_ids"]
        )
        results.append({
            "task": task,
            "chunks": [c.payload for c in chunks],
            "score": hit.score
        })
    
    return results
```

### 5.6 跨 Session 总结

```python
SUMMARY_PROMPT = """你是一个技术写作助手。基于以下多个 session 的相关任务摘要,生成一个主题性总结。

【用户问题】
{query}

【相关任务摘要】
{task_summaries}

【要求】
- 按主题或时间线组织
- 突出关键决策、问题、解决方案
- 引用具体的 session_id/task_id
- 300-800 字
"""

def summarize_sessions(query: str, top_k: int = 10, 
                        time_range: tuple = None) -> str:
    # 1. 检索相关 Task
    results = search_sessions(query, top_k=top_k)
    
    # 2. 可选: 时间范围过滤
    if time_range:
        results = filter_by_time(results, time_range)
    
    # 3. 拼装 prompt
    task_summaries = "\n\n".join([
        f"[{r['task']['task_id']}] {r['task']['task_label']}\n"
        f"{r['task']['task_summary']}"
        for r in results
    ])
    
    prompt = SUMMARY_PROMPT.format(query=query, task_summaries=task_summaries)
    
    # 4. LLM 生成
    return llm_call(prompt)
```

---

## 6. 技术选型

### 6.1 Embedding 模型

| 选项 | 选型 | 原因 |
|------|------|------|
| **主选** | `Qwen/Qwen3-Embedding-0.6B` | 多语言、32K 上下文、效果强、显存可控(1-2G) |
| 备选 | `BAAI/bge-m3` | 100+ 语言、稠密+稀疏+多向量 |
| 不选 | `bge-small-zh-v1.5` | 上下文仅 512,session 长的话要切碎 |
| 不选 | `Qwen3-VL-Embedding-2B` | 图片非必需,白白多 4G 显存 |

**Embedding 维度**:1024

### 6.2 Reranker

`Qwen/Qwen3-Reranker-0.6B`
- 与 Embedding 同源,组合使用效果好
- 显存约 1-2G

### 6.3 LLM(可配置)

通过配置文件指定,默认使用 opencode zen 平台模型:

```yaml
llm:
  provider: "opencode_zen"  # 可换 openai / anthropic / ollama
  model: "deepseek-v4-flash-free"  # 用户指定,实际可用名称以平台为准
  api_key_env: "OPENCODE_ZEN_API_KEY"
  base_url: "https://api.opencode-zen.example/v1"
  max_retries: 3
  timeout: 60
```

**使用场景**:
- Task 清单生成
- 知识图谱三元组抽取
- 实体合并确认
- 跨 session 总结

### 6.4 向量数据库

**Qdrant**(主选)
- 支持 metadata 过滤
- Rust 实现,性能好
- 本地 Docker 部署简单
- Python 客户端友好

### 6.5 图数据库

**Phase 1:NetworkX**(本版本)
- Python 原生,零部署成本
- 适合万级节点以内
- 持久化:`gpickle` / `gexf`
- 可视化方案详见第 11 章(推荐 pyvis)

**后期:Neo4j**(README 中说明)
- 适合 10 万+ 节点
- Cypher 查询语言
- 可视化 Browser
- 迁移路径见第 10 节

---

## 7. 实施路线

### 7.1 Phase 划分

| Phase | 内容 | 时间 | 依赖 | 产出 |
|-------|------|------|------|------|
| **1** | 数据预处理:Session 读取 + 轮次切分 + 内容整理 | 1-2 天 | 无 | `chunker.py` `cleaner.py` |
| **2** | Task 清单生成(LLM 调用 + 解析校验) | 1 天 | Phase 1 | `task_extractor.py` |
| **3** | Qdrant 双 Collection(初始化 + 写入) | 1 天 | Phase 2 | `qdrant_store.py` |
| **4** | 跨 session 搜索(Query → Embedding → Reranker → 返回) | 1 天 | Phase 3 | `searcher.py` |
| **5** | 知识图谱(三元组抽取 + 实体对齐 + NetworkX 写入) | 2-3 天 | Phase 3 | `kg_builder.py` |
| **6** | 跨 session 总结(检索 + LLM 总结) | 1 天 | Phase 3,4 | `summarizer.py` |
| **7** | README + 评估脚本 + 配置化 | 1 天 | 全 | `README.md` `eval/` |

**总周期**:8-10 天可跑通 MVP

### 7.2 优先级建议

**P0(必须先做)**:
- Phase 1:数据预处理(阻塞下游)
- Phase 2:Task 清单生成(核心创新点)
- Phase 3:Qdrant 写入(基础设施)

**P1(MVP 必需)**:
- Phase 4:跨 session 搜索(主功能)

**P2(可后置)**:
- Phase 5:知识图谱
- Phase 6:跨 session 总结
- Phase 7:评估 + 文档

### 7.3 第一个里程碑

**目标**:跑通一个 session,看到全流程产物

**步骤**:
1. 选 1 个真实 session
2. Phase 1 → 切出 N 个 chunk
3. Phase 2 → 生成 1-2 个 task
4. Phase 3 → 写入 Qdrant
5. 手动用 Qdrant Dashboard 看效果
6. 调优 prompt 与整理规则

---

## 8. 风险与缓解

| 风险 | 等级 | 缓解策略 |
|------|------|---------|
| LLM 输出的 Task 数量或粒度不符合预期 | 中 | Prompt 明确约束 + 解析时校验,失败重试 |
| 32K 上下文超限(MCP 返回大量数据) | 中 | Step 2 内容整理优先,Step 4 兜底截断 |
| 实体对齐 LLM 二次确认成本高 | 低 | 仅对相似度 >0.92 的候选对确认,样本少 |
| 失败重试导致 LLM 成本激增 | 低 | 最多 3 次,退避策略 |
| 增量更新冲突(Qdrant + NetworkX 同步) | 中 | 先更新 Qdrant,后更新 NetworkX,失败回滚 Qdrant |
| OpenCode session 数据格式不统一 | 中 | Phase 1 增加 adapter,支持多版本格式 |

### 8.1 一致性保证

- Qdrant 与 NetworkX 的关联:`task_id` 和 `entity_name` 双向一致
- 写入采用"先 Qdrant 后 NetworkX"顺序,失败可补偿

### 8.2 增量更新

```python
def process_session_incremental(session_id: str):
    """处理新 session,跳过已处理的"""
    if session_id in processed_sessions:
        return
    # ... 跑完整流程
    mark_processed(session_id)
```

---

## 9. 评估指标

### 9.1 搜索质量

- 准备 20-30 个真实查询
- 评估 Top-1/Top-3/Top-5 的召回率
- 人工评分相关性(1-5 分)

### 9.2 知识图谱质量

- 抽样 100 个三元组,人工评判正确性
- 实体对齐抽样 50 对,评估归一准确率

### 9.3 总结质量

- 选 5-10 个真实场景
- 人工评估总结的覆盖度、准确性、可读性

### 9.4 性能指标

- 单 session 处理时间(应 < 30s)
- 检索响应时间(应 < 500ms)
- 1 万 session 总体处理时间(预期 < 4 小时)

---

## 10. 后期演进

### 10.1 NetworkX → Neo4j 迁移

**触发条件**:
- 节点数 > 5 万
- 需要复杂 Cypher 查询
- 需要多用户并发访问
- 需要可视化 Browser

**迁移步骤**:
1. 从 NetworkX `gpickle` 导出所有节点和边
2. 转换为 Cypher `CREATE` 语句
3. 批量导入 Neo4j(使用 `neo4j-admin import` 或 `LOAD CSV`)
4. 重建索引
5. 切换读取路径

**接口抽象**:在 Phase 1 就用 `GraphStore` 接口隔离实现,迁移时只换实现不动业务代码。

```python
class GraphStore(ABC):
    @abstractmethod
    def add_node(self, ...): ...
    @abstractmethod
    def add_edge(self, ...): ...
    @abstractmethod
    def query(self, cypher_or_pattern: str): ...

class NetworkXStore(GraphStore): ...
class Neo4jStore(GraphStore): ...
```

### 10.2 其他优化方向

- **多模态**:接入 Qwen3-VL-Embedding,支持图片内容
- **实时更新**:Session 结束时自动触发处理 pipeline
- **多用户/权限**:支持多账号隔离
- **可视化前端**:基于 React + D3.js 的知识图谱浏览器
- **时序分析**:基于时间线的 session 演变追踪
- **Reranker 蒸馏**:用 Qwen3-Reranker 蒸馏小模型,降低推理成本

---

## 11. 可视化方案

知识图谱构建后,需要一套网页可视化方案供用户浏览、查询、交互。本节对比主流方案并给出推荐。

### 11.1 方案对比

| 方案 | 复杂度 | 交互性 | 适合场景 | 推荐度 |
|------|--------|--------|---------|--------|
| **pyvis**(基于 vis.js) | ⭐ 极简 | ⭐⭐⭐ 强 | 快速出图、静态部署 | ⭐⭐⭐⭐⭐ |
| **dash-cytoscape** | ⭐⭐⭐ 中 | ⭐⭐⭐⭐ 很强 | 完整 web app | ⭐⭐⭐⭐ |
| **streamlit-agraph** | ⭐⭐ 简 | ⭐⭐⭐ 中 | 内部 demo | ⭐⭐⭐ |
| d3.js 手动转 JSON | ⭐⭐⭐⭐⭐ 难 | ⭐⭐⭐⭐⭐ 最强 | 完全定制 | ⭐⭐ |
| Neo4j Browser | — | ⭐⭐⭐⭐ 很强 | 后期迁 Neo4j 后 | — |

### 11.2 推荐方案:pyvis(主)+ Dash-Cytoscape(备)

#### 11.2.1 pyvis 主页(默认)

适合:快速生成可分享的 HTML 图,静态部署,无后端服务。

**核心代码**:
```python
from pyvis.network import Network

def visualize_with_pyvis(G, output_html: str = "graph.html") -> str:
    """NetworkX 图 → pyvis 交互式 HTML"""
    net = Network(
        height="800px",
        width="100%",
        bgcolor="#222222",
        font_color="white",
        directed=True,
        notebook=False
    )
    
    # 按节点类型着色
    color_map = {
        "person": "#FF6B6B",
        "module": "#4ECDC4",
        "bug": "#FFE66D",
        "concept": "#95E1D3",
        "file": "#C7CEEA",
        "tool": "#FFDAC1",
    }
    
    for node, attrs in G.nodes(data=True):
        net.add_node(
            node,
            label=node,
            color=color_map.get(attrs.get("type", ""), "#888888"),
            size=15 + G.degree(node) * 2,  # 度越大节点越大
            title=f"<b>{node}</b><br>类型: {attrs.get('type', 'N/A')}<br>"
                  f"关联任务: {len(attrs.get('source_tasks', []))}"
        )
    
    for u, v, attrs in G.edges(data=True):
        net.add_edge(
            u, v,
            label=attrs.get("relation", ""),
            title=attrs.get("relation", ""),
            color="#666666" if attrs.get("weight", 1) > 0.7 else "#333333"
        )
    
    # 物理引擎配置
    net.set_options("""
    var options = {
      "physics": {
        "forceAtlas2Based": {
          "gravitationalConstant": -50,
          "centralGravity": 0.01,
          "springLength": 100,
          "springConstant": 0.08
        },
        "minVelocity": 0.75,
        "solver": "forceAtlas2Based"
      },
      "interaction": {
        "hover": true,
        "tooltipDelay": 100,
        "navigationButtons": true,
        "keyboard": true
      }
    }
    """)
    
    net.save_graph(output_html)
    return output_html
```

**使用**:
```python
# 在 kg_builder.py 末尾
G = build_knowledge_graph(...)
html_path = visualize_with_pyvis(G, "output/knowledge_graph.html")
print(f"图谱已生成: {html_path}")
# 浏览器打开: file:///path/to/knowledge_graph.html
```

**优点**:
- 一行代码生成 HTML,部署零成本
- 节点拖拽、缩放、悬浮显示详情
- 支持万级节点
- 直接发文件给同事,无需起服务

**缺点**:
- 无后端查询能力(只能看,不能动态过滤)
- 节点 > 5 万会卡

#### 11.2.2 Dash + Cytoscape 备选

适合:需要"点击节点查看原始 session"、"搜索高亮"、"动态过滤"等交互时。

**最小实现**:
```python
import dash
from dash import dcc, html, Input, Output
import dash_cytoscape as cyto
import json

app = dash.Dash(__name__)

def nx_to_cytoscape(G):
    elements = []
    for node, attrs in G.nodes(data=True):
        elements.append({
            "data": {"id": node, "label": node, **attrs}
        })
    for u, v, attrs in G.edges(data=True):
        elements.append({
            "data": {"source": u, "target": v, **attrs}
        })
    return elements

app.layout = html.Div([
    html.H1("OpenCode 知识图谱"),
    dcc.Input(id="search", placeholder="搜索实体...", type="text", debounce=True),
    cyto.Cytoscape(
        id="graph",
        elements=nx_to_cytoscape(G),
        style={"width": "100%", "height": "800px", "backgroundColor": "#1a1a1a"},
        layout={"name": "cose"},
        stylesheet=[
            {
                "selector": "node",
                "style": {
                    "label": "data(label)",
                    "color": "#fff",
                    "background-color": "#4ECDC4",
                    "font-size": "12px"
                }
            },
            {
                "selector": "edge",
                "style": {
                    "label": "data(relation)",
                    "curve-style": "bezier",
                    "target-arrow-shape": "triangle",
                    "color": "#aaa",
                    "font-size": "10px"
                }
            },
            {
                "selector": ".highlighted",
                "style": {
                    "background-color": "#FFE66D",
                    "line-color": "#FFE66D",
                    "target-arrow-color": "#FFE66D",
                    "transition-property": "background-color, line-color, target-arrow-color",
                    "transition-duration": "0.5s"
                }
            }
        ]
    ),
    html.Pre(id="node-detail", style={"padding": "20px", "color": "#fff"})
])

@app.callback(
    [Output("graph", "elements"), Output("node-detail", "children")],
    [Input("search", "value"), Input("graph", "tapNodeData")]
)
def update_graph(search, node_data):
    if search:
        # 搜索高亮
        matched = [n for n in G.nodes() if search.lower() in n.lower()]
        elements = nx_to_cytoscape(G.subgraph(matched).copy() if matched else G)
        return elements, ""
    if node_data:
        return dash.no_update, json.dumps(node_data, indent=2, ensure_ascii=False)
    return dash.no_update, ""

if __name__ == "__main__":
    app.run_server(debug=True, port=8050)
```

**部署**:`gunicorn app:server` 或 `python app.py`

#### 11.2.3 Streamlit 备选(快速 demo)

适合:内部 demo,5 分钟搭起来。

```python
import streamlit as st
from streamlit_agraph import agraph, Node, Edge, Config

st.title("知识图谱浏览器")

keyword = st.text_input("搜索节点")

filtered = [n for n in G.nodes() if keyword.lower() in n.lower()] if keyword else list(G.nodes())

nodes = [Node(id=n, label=n, size=20) for n in filtered]
edges = [Edge(source=u, target=v, label=G[u][v].get("relation", ""))
         for u, v in G.edges() if u in filtered and v in filtered]

config = Config(width=1000, height=800, directed=True, physics=True, hierarchical=False)
agraph(nodes=nodes, edges=edges, config=config)
```

### 11.3 选型决策

| 场景 | 推荐方案 | 理由 |
|------|---------|------|
| **MVP / 验证期** | pyvis | 零部署,5 分钟出图,验证 KG 质量 |
| **内部使用 / demo** | Streamlit | 5 分钟搭好,自带搜索 |
| **生产 / 交互复杂** | Dash + Cytoscape | 可点击节点跳原始 session、可过滤、可搜索高亮 |
| **数据量大(>5 万节点)** | Neo4j Browser | pyvis/Dash 都卡,迁 Neo4j |

### 11.4 与知识图谱的接口

在 `kg_builder.py` 中抽象出可视化层:

```python
class KGVisualizer(ABC):
    @abstractmethod
    def render(self, G: nx.Graph, output_path: str) -> str: ...

class PyvisVisualizer(KGVisualizer):
    def render(self, G, output_path="graph.html"):
        # ... 上面的 pyvis 实现
        return output_path

class DashVisualizer(KGVisualizer):
    def render(self, G, output_path="dash_app.py"):
        # 生成 dash app 模板文件
        ...

# 在 config.yaml 中切换
# visualization:
#   backend: "pyvis"  # 或 "dash" / "streamlit"
```

### 11.5 性能考虑

- **< 1 万节点**:pyvis 完全无压力
- **1-5 万节点**:pyvis 略卡,可用子图采样展示
- **> 5 万节点**:必须用 Neo4j Browser
- **简化技巧**:
  - 过滤掉低权重边(`weight < 0.5`)
  - 过滤孤立节点(`degree == 0`)
  - 展示与某实体 N 跳内的子图

```python
def get_subgraph_around(G, center_node: str, n_hops: int = 2) -> nx.Graph:
    """取某节点 N 跳内的子图,避免展示全图卡顿"""
    nodes = {center_node}
    for _ in range(n_hops):
        neighbors = set()
        for n in nodes:
            neighbors.update(G.neighbors(n))
        nodes.update(neighbors)
    return G.subgraph(nodes).copy()
```

### 11.6 集成到 README

README 中应说明:
- 默认生成 `output/knowledge_graph.html`,浏览器直接打开
- 提供 `python -m opencode_kg.visualize --backend dash` 启动交互式
- 数据量大时按上面性能建议处理
- 后期迁移 Neo4j 后,可视化自动切换到 Neo4j Browser

---

## 12. 配置项

```yaml
# config.yaml
project:
  data_dir: "./data"
  output_dir: "./output"
  
opencode:
  session_source: "opencode_export.jsonl"  # 或 API
  session_filter:
    type: "root"
    min_user_messages: 2

chunking:
  boundary: "user_message"
  content_cleaning:
    preserve_full: ["user", "assistant"]
    summarize: ["bash", "tool_call", "mcp_call"]
  max_tokens_per_chunk: 30000

llm:
  provider: "opencode_zen"
  model: "deepseek-v4-flash-free"  # 用户配置,实际名称以平台为准
  api_key_env: "OPENCODE_ZEN_API_KEY"
  base_url: "https://api.opencode-zen.example/v1"
  max_retries: 3
  timeout: 60
  prompt_templates_dir: "./prompts"

embedding:
  model: "Qwen/Qwen3-Embedding-0.6B"
  device: "cuda"  # 或 "cpu" / "mps"
  batch_size: 16
  max_seq_length: 32768

reranker:
  model: "Qwen/Qwen3-Reranker-0.6B"
  device: "cuda"
  top_k: 5

qdrant:
  host: "localhost"
  port: 6333
  collections:
    tasks: "tasks"
    chunks: "chunks"
    entities: "entities"

knowledge_graph:
  store: "networkx"  # 后期可换 "neo4j"
  path: "./output/knowledge_graph.gpickle"
  entity_alignment_threshold: 0.92

visualization:
  backend: "pyvis"  # 可选: pyvis | dash | streamlit
  output_path: "./output/knowledge_graph.html"
  # pyvis 专用配置
  pyvis:
    height: "800px"
    width: "100%"
    bgcolor: "#222222"
    font_color: "white"
    physics_solver: "forceAtlas2Based"  # forceAtlas2Based | barnesHut | repulsion
  # 节点配色(按实体类型)
  node_colors:
    person: "#FF6B6B"
    module: "#4ECDC4"
    bug: "#FFE66D"
    concept: "#95E1D3"
    file: "#C7CEEA"
    tool: "#FFDAC1"
    default: "#888888"
  # 性能控制
  performance:
    max_nodes: 10000  # 超过则子图采样
    max_edges: 30000
    min_edge_weight: 0.5  # 过滤低权重边
    drop_isolated_nodes: true  # 过滤孤立节点
  # dash 专用配置(当 backend=dash 时生效)
  dash:
    host: "0.0.0.0"
    port: 8050
    debug: false

search:
  top_k_tasks: 20
  top_k_chunks: 5
  rerank_top_k: 5

logging:
  level: "INFO"
  file: "./logs/app.log"
```

---

## 13. 附录

### 13.1 项目结构

```
opencode-session-kg/
├── README.md
├── requirements.txt
├── config.yaml
├── prompts/
│   ├── task_extraction.txt
│   ├── triple_extraction.txt
│   ├── entity_merge_confirm.txt
│   └── summary_generation.txt
├── src/
│   ├── __init__.py
│   ├── config.py
│   ├── session_loader.py
│   ├── chunker.py
│   ├── content_cleaner.py
│   ├── task_extractor.py
│   ├── embedder.py
│   ├── qdrant_store.py
│   ├── reranker.py
│   ├── kg_builder.py
│   ├── searcher.py
│   ├── summarizer.py
│   └── utils/
│       ├── token_counter.py
│       └── logger.py
├── data/
│   ├── raw/                  # 原始 session
│   └── processed/            # 处理后
├── output/
│   ├── knowledge_graph.gpickle
│   └── eval/
├── logs/
└── tests/
    ├── test_chunker.py
    ├── test_task_extractor.py
    └── ...
```

### 13.2 Prompt 模板

#### 任务抽取(已列于 5.2.1)

#### 三元组抽取(已列于 5.4.1)

#### 实体合并确认

```python
ENTITY_MERGE_PROMPT = """判断以下两个实体是否应该合并为同一个概念。

实体 A:{entity_a}
实体 B:{entity_b}

合并规则:
- 完全同义或一个为另一个的别名:合并
- 含义不同或属于不同上下文:不合并

仅回答 "MERGE" 或 "KEEP"。
"""
```

#### 跨 Session 总结(已列于 5.6)

### 13.3 依赖列表

```
# requirements.txt
qdrant-client>=1.7.0
sentence-transformers>=2.7.0
networkx>=3.2
tiktoken>=0.5.0
pydantic>=2.0
openai>=1.30.0  # 或 opencode-zen-sdk
pyyaml>=6.0
loguru>=0.7.0
tqdm>=4.66.0
```

### 13.4 评估脚本示例

```python
# eval/search_quality.py
def evaluate_search_quality(test_queries: list[dict]):
    """
    test_queries: [
        {"query": "我之前是怎么修 token 过期 bug 的?",
         "expected_session_ids": ["abc", "def"],
         "expected_chunk_ids": ["abc_c2"]},
        ...
    ]
    """
    results = []
    for tq in test_queries:
        search_results = search_sessions(tq["query"], top_k=5)
        retrieved_sessions = {r["task"]["session_id"] for r in search_results}
        
        recall_5 = len(retrieved_sessions & set(tq["expected_session_ids"])) / \
                   len(tq["expected_session_ids"])
        
        results.append({"query": tq["query"], "recall@5": recall_5})
    
    avg_recall = sum(r["recall@5"] for r in results) / len(results)
    print(f"Average Recall@5: {avg_recall:.2%}")
```

---

## 14. 决策记录(ADR)

### ADR-001:使用 LLM 整 session 视角生成 Task 清单

**背景**:需要把 chunk 按"任务"归并

**选项**:
- A. 按 Embedding 相似度合并
- B. 让 LLM 在每 chunk 打 task_id
- C. **让 LLM 整 session 视角生成 task 清单** ✅

**决定**:采用 C

**理由**:
- 大多数 session 任务数 < 5,全局视角最准
- 避免相似度判别误差
- Prompt 可显式约束任务数量

### ADR-002:Chunk 也向量化,与 Task 双 Collection

**背景**:检索是粗排用 Task,精排需要回到原文

**决定**:Qdrant_T(粗)+ Qdrant_C(细),通过 task_id 关联

**理由**:
- 粒度互补,既保召回又保精度
- 存储开销可接受(chunk 数 < task 数 × 5)

### ADR-003:Phase 1 用 NetworkX,后期可迁 Neo4j

**背景**:图库选型

**决定**:先 NetworkX,接口抽象预留 Neo4j

**理由**:
- Phase 1 数据量小,NetworkX 够用
- 零部署,开发快
- 抽象 `GraphStore` 接口,后期无缝迁移

### ADR-004:LLM 可配置,默认用 opencode zen 平台

**背景**:不同 LLM 成本与效果差异大

**决定**:通过 `config.yaml` 配置 provider 和 model

**理由**:
- 默认 `deepseek-v4-flash-free`(免费)
- 用户可换 GPT-4o-mini、Claude Haiku 等
- 减少对单一平台依赖

---

## 15. 文档版本

| 版本 | 日期 | 变更 |
|------|------|------|
| v1.0 | 2026-07-13 | 初版,基于讨论整合 |
| v2.0 | 2026-07-13 | 整合用户反馈,Task 分组改用 LLM 整 session 视角,内容整理规则细化,NetworkX 优先 |
| v2.1 | 2026-07-13 | 新增第 11 章"可视化方案",推荐 pyvis 为主、Dash-Cytoscape 为备,补齐 README 集成说明 |
| v2.2 | 2026-07-13 | 12. 配置项新增 `visualization` 块,支持 pyvis/dash/streamlit 切换、节点配色、性能控制 |

---

**TODO(实施前需确认)**:
- [ ] OpenCode session 实际数据格式(API/JSON/JSONL)
- [ ] opencode zen 平台可用模型名清单
- [ ] Qwen3-Embedding-0.6B 在 macOS/Linux/Windows 上的兼容性
- [ ] 评估用的真实 session 样本量
